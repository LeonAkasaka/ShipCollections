namespace ShipCollections
{
    class Program
    {
        static void Main(string[] args)
        {
            var ships = Ship.LoadAll();
            var types = ShipType.LoadAll();

            //以下にデータ処理や演習問題を書きます。
        }
    }
}
